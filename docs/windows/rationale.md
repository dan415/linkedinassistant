

<h2> Windows Service </h2>

This module contains the code that is used to run the application as a Windows service. It uses the
code on the main module to run the different services, and it also contains the code that is used
to install the service, and to respond to the different signals that are sent to the service.